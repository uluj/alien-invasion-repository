
// Create event code for Object31
y = offset2;

// Check if obj_s4 exists before accessing its properties
if (instance_exists(obj_s4)) {
    obj_s4.image_blend = c_red;
}

// Check if obj_s1 exists before accessing its properties
if (instance_exists(obj_s1)) {
    obj_s1.image_blend = c_red;
}
if (instance_exists(obj_s10)) {
    obj_s10.image_blend = c_red;
}
